name: Keep API awake

on:
  schedule:
    - cron: '*/10 3-14 * * 0,1,2,3,4'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS --max-time 120 --retry 3 --retry-all-errors "$API_URL/health"
        env:
          API_URL: ${{ vars.VITE_API_URL }}
